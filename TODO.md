- CLI to manage kiosk mode
- unlock TTY switching
- How to create overlayer addon? for example, simple CPU chart on the right side, for developer purposes
- semantic versioning + pipeline building artifact and releasing when tag was commited
- license MIT
- Logging system: capture console logs (log/warn/error), network requests
  (Fetch/XHR with request/response payloads), and JS exceptions from the
  running page. Consider: WebKit C API signals for basic logs to journald,
  JS injection or Inspector Protocol client for full network capture to file.